% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/csam-generics.R
\name{vcov.csam2}
\alias{vcov.csam2}
\title{Variance-covariance estimators for csam fits (analytic, modular)}
\usage{
\method{vcov}{csam2}(object, method = c("naive", "louis", "oakes", "sandwich"), ...)
}
\arguments{
\item{object}{A fitted object of class \code{"csam"} (must contain \code{Y}, \code{X},
\code{psi1}, and \code{psi2}).}

\item{method}{Character; one of \code{"naive"}, \code{"louis"}, \code{"oakes"}, \code{"sandwich"}.}

\item{...}{Currently unused.}
}
\value{
A symmetric variance-covariance matrix for the full parameter
vector (on the unconstrained scale). Row/column names indicate the
parameter ordering.
}
\description{
S3 method for the base \code{stats::vcov} generic that computes variance-covariance
matrices for all model parameters in a fitted \code{"csam"} object using analytic
complete-data scores and expected Hessians. Supported \code{method} values are
\code{"naive"}, \code{"louis"}, \code{"oakes"}, and \code{"sandwich"}.
}
\details{
The returned matrix is on an unconstrained parameter scale:
\itemize{
\item \strong{beta0}: raw (length s)
\item \strong{B}: stacked by archetype then predictor (length g * p)
\item \strong{pi}: log-odds for first (g-1) components vs component g
\item \strong{U}: stacked site scores (length n * d)
\item \strong{Lambda}: stacked loadings (length s * d)
\item \strong{phi}: log(phi) (length s) for Gaussian family
}

The fitted \code{csam} object \strong{must} contain \code{Y}, \code{X}, and the penalty values
\code{psi1} and \code{psi2} (these are inferred from the object and therefore not
required as arguments to \code{vcov}).
}
\examples{
\dontrun{
fit <- csam(Y, X, g = 2, d = 1, family = poisson(), trace = TRUE)
fit$Y <- Y; fit$X <- X
# csam now stores psi1 and psi2 internally
V_louis <- vcov(fit, method = "louis")
}

}
