% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/csam-fit.R
\name{sam}
\alias{sam}
\title{Fit a vanilla Species Archetype Model (SAM)}
\usage{
sam(
  Y,
  X,
  g = 3,
  family = poisson(),
  max_iter = 100,
  tol = 1e-06,
  verbose = TRUE,
  start = NULL,
  maxit_step1 = 5,
  maxit_step2 = 5,
  first_maxit_step1 = 50,
  first_maxit_step2 = 50,
  trace = TRUE
)
}
\arguments{
\item{Y}{A numeric matrix of responses of dimension \eqn{n \times s},
with rows corresponding to sites and columns to species.}

\item{X}{A numeric matrix of predictors of dimension \eqn{n \times p}.}

\item{g}{Integer; number of archetypes (mixture components).}

\item{family}{A GLM family object (e.g. \code{\link[stats:family]{stats::poisson()}}, \code{\link[stats:family]{stats::binomial()}},
\code{\link[stats:family]{stats::gaussian()}}) specifying the exponential family and link.}

\item{max_iter}{Integer; maximum number of ECM iterations.}

\item{tol}{Numeric; convergence tolerance on the penalized log-likelihood.}

\item{verbose}{Logical; if \code{TRUE}, prints the penalized log-likelihood at
each iteration.}

\item{start}{Optional list of initial values with elements
\code{beta0}, \code{B}, \code{pi}, \code{phi}. Any missing elements are
initialized internally.}

\item{maxit_step1}{Integer; maximum P-IRLS iterations in the archetype
update step.}

\item{maxit_step2}{Integer; maximum P-IRLS iterations in the species
parameter update step.}

\item{trace}{Logical; if \code{TRUE}, stores parameter and likelihood traces
across ECM iterations for diagnostics and plotting.}
}
\value{
An object of class \code{"csam"} with components:
\describe{
\item{beta0}{Length-\(s\) vector of species intercepts.}
\item{B}{\eqn{g \times p} matrix of archetype-specific regression
coefficients.}
\item{pi}{Length-\(g\) vector of mixing proportions.}
\item{phi}{Length-\(s\) vector of species-specific dispersion parameters
(for Gaussian family).}
\item{tau}{\eqn{s \times g} matrix of posterior species–archetype
memberships.}
\item{penalized_loglik}{Final log-likelihood value. NOTE this is labelled "penalized" only for software compatibility with downstream functions for \code{csam}}
\item{iterations}{Number of ECM iterations performed.}
\item{family}{The GLM family used.}
\item{trace}{If \code{trace = TRUE}, a list containing parameter and
likelihood traces across iterations.}
}
}
\description{
Fits the species archetype model
\deqn{
  h(\mu_{ijk}) = \beta_{0j} + \mathbf{x}_i^\top \boldsymbol{\beta}_k
}
for species \eqn{j = 1,\ldots,s}, sites \eqn{i = 1,\ldots,n}, and archetypes
\eqn{k = 1,\ldots,g}, where \eqn{h} is the GLM link function. The data are
modeled as a finite mixture of regression models with component densities
from a common exponential family.

The log-likelihood is
\deqn{
  \ell\left(\boldsymbol{\theta};\boldsymbol{Y},\boldsymbol{X}\right) =\sum_{j=1}^{s}\log\left\{\sum_{k=1}^{g}\pi_{k}\prod_{i=1}^{n}f\left(y_{ij}|\mu_{ijk},\phi_{j}\right)\right\}
}

Estimation proceeds via an ECM algorithm with:
\itemize{
\item an E-step updating posterior species–archetype memberships
\eqn{\tau_{jk}},
\item two conditional M-steps updating archetype coefficients
\eqn{\boldsymbol{B}}, and species-specific parameters
\eqn{(\boldsymbol{\beta}_0, \boldsymbol{\phi})} using IRLS.
}
}
\examples{
\dontrun{
set.seed(1)
n <- 50; s <- 10; p <- 2
X <- cbind(1, scale(matrix(rnorm(n * (p - 1)), n, p - 1)))
Y <- matrix(rpois(n * s, lambda = 5), n, s)
fit <- sam(Y, X, g = 2, d = 1, family = poisson(), trace = TRUE)
plot.csam(fit, param = "ll")
}

}
