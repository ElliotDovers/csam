% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/csam-generics.R
\name{plot.csam}
\alias{plot.csam}
\title{Plot diagnostics for CSAM fits}
\usage{
\method{plot}{csam}(
  x,
  param = c("pll", "B", "beta0", "pi", "U", "Lambda", "phi", "cor"),
  truth = NULL,
  ylim = NULL,
  ...
)
}
\arguments{
\item{x}{A fitted object of class \code{"csam"}.}

\item{param}{Character; which quantity to plot. See Details.}

\item{truth}{Optional list of true parameter values (named like the
components of a \code{"csam"} object) to overlay as horizontal reference
lines or to show the true correlation matrix in the upper triangle.}

\item{ylim}{Optional numeric vector of length 2 giving y-axis limits.}

\item{...}{Additional arguments (currently ignored).}
}
\description{
S3 method for the base \code{graphics::plot} generic that produces ECM traces
and diagnostic plots for objects of class \code{"csam"}. Available \code{param}
options are:
\itemize{
\item \code{"pll"}: penalised log-likelihood trace,
\item \code{"B"}: archetype coefficient traces (one panel per predictor),
\item \code{"beta0"}: species intercept traces,
\item \code{"pi"}: mixing proportion traces,
\item \code{"U"}: site-score traces (one panel per factor),
\item \code{"Lambda"}: species-loading traces (one panel per factor),
\item \code{"phi"}: species dispersion traces (Gaussian family only),
\item \code{"cor"}: correlation matrix induced by the estimated loadings
(requires \code{corrplot} for a prettier display).
}
}
\details{
This function expects the fitted object to contain a \code{trace} element
produced when the model was fit with \code{trace = TRUE}. The trace should
contain lists of parameter values across ECM iterations (see the
package documentation for the structure).
}
\examples{
\dontrun{
fit <- csam(Y, X, g = 2, d = 1, family = poisson(), trace = TRUE)
plot(fit, param = "pll")
plot(fit, param = "cor")
}

}
